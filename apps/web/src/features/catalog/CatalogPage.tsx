import { FeaturePlaceholder } from "../shared/FeaturePlaceholder";

export function CatalogPage() {
  return <FeaturePlaceholder title="Catálogo" pendingContract="inventario, productos y recetas" />;
}
